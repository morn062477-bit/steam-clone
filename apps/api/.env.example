# 이 파일을 .env 로 복사한 뒤 실제 값을 채운다. .env 는 커밋되지 않는다.

# AWS RDS (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require"

# 로컬 embedded-postgres 로 되돌릴 때
# DATABASE_URL="postgresql://postgres@localhost:5433/steamclone?schema=public"

# 서버 포트 (기본 3000)
# PORT=3000
